// Task for launching main.exe located in cwd
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows Launch",
            "program": "${workspaceFolder}/main_debug.exe",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}